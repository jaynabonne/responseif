.define fadein .transition fadein .enddef
.responses room
	.response
		.matches LOOK
		.runs 1
		.does
			.says
				<div id="intro">
					<div id="introrain"></div>
					<div id="introwind"></div>
					<div id="introdream">{!You dream.|INTROFIRST!}</div>
				</div>
			.fadein
	.response
		.matches INTROFIRST
		.does .says {!The patter of rain...|INTRORAIN!} .fadein .into #introrain
	.response
		.matches INTROFIRST
		.does .says {!A caress of wind...|INTROWIND!} .fadein .into #introwind
	.response
		.matches INTROFIRST
		.does .says {!You dream...|INTRODREAM!} .fadein .into #introdream

	.response	
		.matches INTRORAIN
		.does .says The patter of rain... but there is no rain. .fadein .into #introrain
	.response	
		.matches INTROWIND
		.does .says A caress of wind... but there is no wind. .fadein .into #introwind
	.response	
		.matches INTRODREAM
		.does .says You dream...<br>but you should not dream. .fadein .into #introdream 
		
.end
